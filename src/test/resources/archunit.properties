# Skeleton starts with empty hexagon rings; the A1–A7 / jMolecules rules match no classes yet.
# Allow an empty should so the gate passes on the bare skeleton and activates automatically as the
# first slice populates the rings (walking-skeleton onward).
archRule.failOnEmptyShould=false
